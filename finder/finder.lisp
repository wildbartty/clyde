(in-package :clyde-finder)

(let ((current-package *package*)
      (
(defun list-all-symbols (&optional (package *package*))
  (loop for x being each symbol in package collect (format nil "~a" x)))
  
